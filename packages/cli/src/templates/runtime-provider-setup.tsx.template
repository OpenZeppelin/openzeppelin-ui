/**
 * OpenZeppelin UI Kit — Provider wiring
 * Generated by: oz-ui migrate init
 *
 * This file sets up RuntimeProvider and WalletStateProvider at the root of your app.
 * RuntimeProvider manages adapter lifecycle; WalletStateProvider tracks connected wallets.
 * Keep this provider tree stable during migration so UI and wallet tasks can
 * incrementally switch to capability-based consumption without breaking the app.
 */

import { RuntimeProvider, WalletStateProvider } from '@openzeppelin/ui-react';
import type { ReactNode } from 'react';

import { resolveRuntime } from './resolve-runtime';

interface OzProvidersProps {
  children: ReactNode;
}

export function OzProviders({ children }: OzProvidersProps) {
  return (
    <RuntimeProvider resolveRuntime={resolveRuntime}>
      <WalletStateProvider>
        {children}
      </WalletStateProvider>
    </RuntimeProvider>
  );
}
