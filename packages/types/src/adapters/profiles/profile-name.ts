/**
 * Supported profile identifiers for {@link EcosystemExport.createRuntime}.
 *
 * Each profile composes a fixed subset of the 13 capabilities for a common app archetype.
 */
export type ProfileName = 'declarative' | 'viewer' | 'transactor' | 'composer' | 'operator';
